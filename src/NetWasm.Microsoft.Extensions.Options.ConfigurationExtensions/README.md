# NetWasm.Microsoft.Extensions.Options.ConfigurationExtensions

Generated-binder integration between configuration and Options. Use the
`Bind(IConfiguration)` overloads; `BindConfiguration` needs an ambient
`IConfiguration` service and is intentionally rejected in this profile.
