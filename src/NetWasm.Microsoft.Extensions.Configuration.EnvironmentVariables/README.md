# NetWasm.Microsoft.Extensions.Configuration.EnvironmentVariables

Environment-variable configuration for the NetWasm host. Prefix filtering,
double-underscore path normalization, and connection-string prefixes are
supported; values come from the host environment.
