# NetWasm.Microsoft.Extensions.Configuration

Layered configuration, in-memory providers, sections, and reload tokens for
`netwasm0.1`. File providers and filesystem watchers are intentionally absent.
