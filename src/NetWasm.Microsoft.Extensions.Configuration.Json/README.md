# NetWasm.Microsoft.Extensions.Configuration.Json

JSON stream configuration for `netwasm0.1`. `AddJsonStream` parses a supplied
stream once; physical files and reload-on-change watchers are outside this
profile until the filesystem capability is qualified.
